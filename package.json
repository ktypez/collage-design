{
  "name": "collage-design",
  "version": "1.0.0",
  "description": "Design Gallery \u2014 central design library (themes, design system, review)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "copy:backend": "node build.js",
    "check": "node build.js --check"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0",
    "svgo": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}