{
  "name": "design-gallery",
  "version": "1.0.0",
  "description": "Design Gallery — central design library (themes, design system)",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node build.js --check"
  },
  "engines": {
    "node": ">=18"
  }
}
