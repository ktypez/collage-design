{
  "name": "collage-design",
  "version": "1.0.0",
  "description": "Collage Maker brand refresh design lab \u2014 4 themed concepts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "preview": "node build.js --serve",
    "serve": "node build.js --serve",
    "clean": "node build.js --clean",
    "copy:backend": "node build.js --copy",
    "size": "node build.js --size"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0",
    "svgo": "^3.3.2"
  },
  "engines": {
    "node": ">=18"
  }
}